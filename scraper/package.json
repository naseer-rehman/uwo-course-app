{
  "name": "scraper",
  "module": "./src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "",
    "start": "",
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildtest": "bun build --outdir=build --target=node --sourcemap src/main.ts && bun run build/main.js"
  },
  "devDependencies": {
    "@types/axios": "^0.14.4",
    "@types/bun": "^1.3.9",
    "@types/cheerio": "^1.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "html-format": "^1.1.7"
  }
}
